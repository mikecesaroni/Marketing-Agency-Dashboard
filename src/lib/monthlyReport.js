// Re-exported from the shared module the Edge Function also uses.
// See supabase/functions/_shared/report.ts.
export {
  validEmail,
  reportMonth,
  previousMonth,
  eligibility,
  buildReportModel,
  renderReportHtml,
  renderSubject,
} from '../../supabase/functions/_shared/report.ts'
